// NOTE: Cannot use ACI agents because build requires `unzip` to be available
buildPlugin(useAci: false, configurations: [
        [ platform: "linux", jdk: "8" ],
        [ platform: "windows", jdk: "8" ],
        [ platform: "linux", jdk: "11", jenkins: "2.176.4" ]
])
