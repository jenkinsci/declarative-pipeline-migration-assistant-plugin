<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:l="/lib/layout" xmlns:f="/lib/form">
  
  <l:layout title="${it.displayName}">
    <l:main-panel>
      <f:form name="convert" action="convert" method="post">
        <h1>${it.displayName}</h1>
        <f:section title="${%Generate}">
          <f:entry title="Only Generate Jenkinsfile" field="jenkinsFileOnly">
            <f:checkbox name="jenkinsFileOnly" field="jenkinsFileOnly" default="false"/>
          </f:entry>
          <f:entry title="${%newName}" field="newName">
            <f:textbox name="newName" field="newName"/>
          </f:entry>
          <f:block>
            <f:submit name="Submit" value="${%submit}"/>
          </f:block>
        </f:section>
      </f:form>
    </l:main-panel>
  </l:layout>
</j:jelly>
